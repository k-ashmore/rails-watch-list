<h1>New</h1>
<div class="container mt-5">
  <h1>Add a new list</h1>

  <%= simple_form_for @list do |f| %>
    <%= f.input :name %>
    <%= f.input :photo, as: :file %>
    <%= f.button :submit %>
  <% end %>
</div>
